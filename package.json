{
  "name": "vue-autosize-textarea",
  "version": "0.0.1",
  "description": "Autosize textarea for Vue 2 with proper v-model bindings",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "rimraf dist/* && babel --presets es2015 -d dist/ src/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nerdinacan/vue-autosize-textarea.git"
  },
  "keywords": [],
  "author": "Mason Houtz <nerdinacan@gmail.com>",
  "license": "ISC",
  "devDependencies": {
    "babel-cli": "^6.24.0",
    "babel-preset-es2015": "^6.24.0",
    "rimraf": "^2.6.1"
  },
  "dependencies": {
    "autosize": "^3.0.20"
  }
}
